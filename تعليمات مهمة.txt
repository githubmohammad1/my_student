ممتاز يا محمد! إليك قائمة مختصرة وسريعة لأهم أوامر **سطر الأوامر (Shell / Terminal)** للتعامل مع **الملفات والمجلدات** بشكل عام، سواء كنت تستخدم Linux أو macOS أو حتى Windows (عبر Git Bash أو WSL):

---

## 📁 أوامر المجلدات

- إنشاء مجلد:  
  ```bash
  mkdir اسم_المجلد
  ```

- إنشاء مجلدات متداخلة:  
  ```bash
  mkdir -p مجلد1/مجلد2/مجلد3
  ```

- عرض محتويات المجلد:  
  ```bash
  ls
  ```

- الدخول إلى مجلد:  
  ```bash
  cd اسم_المجلد
  ```

- الرجوع للخلف:  
  ```bash
  cd ..
  ```

- الرجوع للمجلد السابق مباشرة:  
  ```bash
  cd -
  ```

---

## 📄 أوامر الملفات

- إنشاء ملف فارغ:  
  ```bash
  touch اسم_الملف.py
  ```

- تحرير ملف (باستخدام محرر nano):  
  ```bash
  nano اسم_الملف.py
  ```

- نسخ ملف:  
  ```bash
  cp الملف_الأصلي الملف_المنسوخ
  ```

- نقل أو إعادة تسمية ملف:  
  ```bash
  mv الملف_القديم الملف_الجديد
  ```

- حذف ملف:  
  ```bash
  rm اسم_الملف
  ```

- حذف مجلد فارغ:  
  ```bash
  rmdir اسم_المجلد
  ```

- حذف مجلد بمحتوياته:  
  ```bash
  rm -r اسم_المجلد
  ```

---

## 📦 أوامر إضافية مفيدة

- عرض المسار الحالي:  
  ```bash
  pwd
  ```

- إنشاء ملف وكتابة محتوى فيه مباشرة:  
  ```bash
  echo "نص داخل الملف" > اسم_الملف.txt
  ```

- عرض محتوى ملف:  
  ```bash
  cat اسم_الملف.txt
  ```

- البحث عن ملف:  
  ```bash
  find . -name "اسم_الملف"
  ```

---

هل ترغب بإضافة أوامر خاصة بالبحث، الأذونات، أو التعامل مع الملفات النصية؟
 del db.sqlite3 
  del quran_school\migrations\0*.py

  py.exe .\manage.py createsuperuser
  بالطبع يا محمد ✅  
بما أن تطبيقك يحتوي على قسم **واجهات برمجية API** مبني بـ **Django REST Framework**، فسأعطيك **قائمة منظمة** لكل نقاط النهاية (Endpoints) التي يمكن لتطبيق Flutter استهلاكها، مع نوع الطلب (HTTP Method) ووصف مختصر لما تفعله.

---

## **قائمة الـ Endpoints**

### **👤 الطلاب (Students)**
- **GET** `/api/students/` → جلب قائمة الطلاب.
- **POST** `/api/students/` → إضافة طالب جديد.
- **GET** `/api/students/<id>/` → جلب بيانات طالب محدد.
- **PUT** `/api/students/<id>/` → تعديل بيانات طالب.
- **DELETE** `/api/students/<id>/` → حذف طالب.

---

### **📝 الاختبارات (Tests)**
- **GET** `/api/tests/` → جلب قائمة الاختبارات.
- **POST** `/api/tests/` → إضافة اختبار جديد.
- **GET** `/api/tests/<id>/` → جلب بيانات اختبار محدد.
- **PUT** `/api/tests/<id>/` → تعديل بيانات اختبار.
- **DELETE** `/api/tests/<id>/` → حذف اختبار.

---

### **📅 الحضور (Attendance)**
- **GET** `/api/attendances/` → جلب سجل الحضور.
- **POST** `/api/attendances/` → تسجيل حضور جديد.
- **GET** `/api/attendances/<id>/` → جلب سجل حضور محدد.
- **PUT** `/api/attendances/<id>/` → تعديل سجل الحضور.
- **DELETE** `/api/attendances/<id>/` → حذف سجل الحضور.

---

### **📢 الإعلانات (Announcements)**
- **GET** `/api/announcements/` → جلب قائمة الإعلانات.
- **POST** `/api/announcements/` → إضافة إعلان جديد.
- **GET** `/api/announcements/<id>/` → جلب إعلان محدد.
- **PUT** `/api/announcements/<id>/` → تعديل إعلان.
- **DELETE** `/api/announcements/<id>/` → حذف إعلان.

---

### **💰 المدفوعات (Payments)**
- **GET** `/api/payments/` → جلب قائمة المدفوعات.
- **POST** `/api/payments/` → إضافة دفعة جديدة.
- **GET** `/api/payments/<id>/` → جلب دفعة محددة.
- **PUT** `/api/payments/<id>/` → تعديل دفعة.
- **DELETE** `/api/payments/<id>/` → حذف دفعة.

---

### **📈 التقدم (Progress)**
- **GET** `/api/progress/` → جلب قائمة سجلات التقدم.
- **POST** `/api/progress/` → إضافة سجل تقدم جديد.
- **GET** `/api/progress/<id>/` → جلب سجل تقدم محدد.
- **PUT** `/api/progress/<id>/` → تعديل سجل التقدم.
- **DELETE** `/api/progress/<id>/` → حذف سجل التقدم.

---

## 💡 ملاحظات مهمة لتطبيق Flutter
1. **التعامل مع JSON**: كل هذه المسارات ترجع بيانات JSON، لذا ستحتاج لاستخدام مكتبات مثل `http` أو `dio` في Flutter.
2. **المصادقة (Authentication)**: إذا أضفت لاحقًا حماية JWT أو Token، ستحتاج لإضافة Headers مع الطلبات.
3. **بنية URL الأساسية**: إذا كان الـ API يعمل على سيرفر محلي، استخدم:
   ```
   http://127.0.0.1:8000
   ```
   أو إذا كان على استضافة خارجية ضع رابط الدومين.
4. **التعامل مع الأخطاء**: تأكد من فحص أكواد الاستجابة (200, 201, 400, 404…) لمعالجة النتائج.

---
